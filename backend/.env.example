PORT=4000
NODE_ENV=production
MONGODB_URI=
# openssl rand -hex 64 (use different values for access vs refresh; never commit real secrets)
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
FRONTEND_URL=
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=
