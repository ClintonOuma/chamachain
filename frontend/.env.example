# Required for production (Vercel/Netlify env at BUILD). Origin only, no /api/v1.
# Example: https://your-api.up.railway.app
VITE_API_URL=

# Optional dev-only: direct API origin (default uses Vite proxy /api -> localhost:4000)
# VITE_DEV_API_URL=http://localhost:4000
